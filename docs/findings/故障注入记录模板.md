# 故障注入记录:<故障名>

> 本目录的内容必须是真实跑出来的结果。不写预测、不写模拟、不写示例数据。
> 每次注入复制本模板为一个新文件,文件名:`YYYYMMDD-<故障>-注入.md`。
> 注入方式限定为手动 `docker compose stop / start <服务>`(CLAUDE.md §3)。

## 1. 目标

- 注入的故障(停哪个容器):
- 想验证的问题(一句话):
- 预期行为(注入前写下,注入后不许改):
  - 接口层:
  - 数据层:
  - 指标 / 日志:

## 2. 环境

| 项 | 值 |
|---|---|
| 日期 / 时间 | |
| 服务版本(git commit / tag) | |
| 服务运行方式 | |
| `llm.mode` | |
| 注入前 `docker compose ps` 输出 | |

## 3. 步骤(按实际执行顺序,带时间戳)

| 时间 | 操作 | 命令 |
|---|---|---|
| | 基线:正常调用一次 | |
| | 注入:`docker compose stop <服务>` | |
| | 注入期间调用 | |
| | 恢复:`docker compose start <服务>` | |
| | 恢复后调用 | |

## 4. 观察

### 4.1 接口响应(粘贴 curl 原始输出,含 HTTP 状态、code、traceId)

```
(粘贴)
```

### 4.2 `/actuator/health` 在注入期间的输出

```
(粘贴)
```

### 4.3 指标变化(写明指标名、注入前后的值)

| 指标 | 注入前 | 注入中 | 恢复后 |
|---|---|---|---|
| | | | |

### 4.4 日志片段(按 traceId 过滤)

```
(粘贴)
```

### 4.5 数据核对(SQL 与结果)

```
(粘贴)
```

## 5. 结论

- 预期 vs 实际:
- 降级 / 隔离是否按设计工作:
- 恢复后是否自愈,自愈耗时:
- 发现的问题:
- 对应的 ADR 是否需要修订:
