# 压测记录:<场景名>

> 本目录的内容必须是真实跑出来的结果。不写预测、不写模拟、不写示例数据。
> 每次压测复制本模板为一个新文件,文件名:`YYYYMMDD-<场景>-压测.md`。

## 1. 目标

- 被测接口:
- 想验证的问题(一句话):
- 预期结果(压测前写下,压测后不许改):

## 2. 环境

| 项 | 值 |
|---|---|
| 日期 / 时间 | |
| 服务版本(git commit / tag) | |
| 服务运行方式(mvn spring-boot:run / docker profile app) | |
| JVM 参数 | |
| MySQL / Redis / RabbitMQ / WireMock 版本与运行方式 | |
| `llm.mode` | |
| 机器(CPU / 内存 / 是否与被测服务同机) | |
| JMeter 版本 | |

## 3. 压测配置

| 项 | 值 |
|---|---|
| JMeter 脚本路径(`tests/perf/...`) | |
| 线程数 | |
| Ramp-up | |
| 循环次数 / 持续时间 | |
| 前置数据(工单数量、状态、坐席) | |
| 断言 | |

## 4. 结果

### 4.1 JMeter 汇总(粘贴 Aggregate Report 或 Summary Report 原始数据)

```
(粘贴)
```

### 4.2 服务侧指标(Prometheus / Actuator,写明查询语句和时间范围)

```
(粘贴)
```

### 4.3 数据库核对(写明 SQL 和结果)

```sql
-- 例:SELECT ... FROM ticket WHERE id = ?;
```

```
(粘贴)
```

### 4.4 审计日志 / MQ 去重表核对

```
(粘贴)
```

## 5. 观察到的现象

- 
- 

## 6. 结论

- 预期 vs 实际:
- 是否发现缺陷:
- 缺陷根因(如果定位到了):
- 修复方案(只写方案,修复在代码仓库单独提交并回填 commit):

## 7. 附件

- JMeter `.jtl` 文件路径:
- 截图:
- 服务日志片段路径:
