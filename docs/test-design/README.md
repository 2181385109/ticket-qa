# 用例设计文档索引

每一份按"用了什么方法、为什么用它、推导出哪些用例、对应哪个测试文件"写。方法名用标准术语:

| 方法 | 一句话 | 本项目用在哪 |
|---|---|---|
| 状态迁移法 | 画出状态迁移表,表里每一格是一条用例:合法格测放行和副作用,非法格测拒绝 | 工单状态机(01) |
| 边界值分析 | 对有序输入域取 边界点、边界 ± 1 | SLA 闭区间、重开 7 天窗口、附件 5MB、标题 200 字、分页(02、05) |
| 判定表法 | 多个条件的组合决定动作,每一行一条用例 | LLM 降级 / 熔断 / 契约(03)、权限(04) |
| 等价类划分 | 输入域切成有效 / 无效类,每类取一个代表 | 附件三层校验、参数校验、注入载荷、鉴权头(05、07) |
| 场景法 | 按业务流程的主路径 + 备选路径组织,一条场景一条用例 | MQ 幂等、SLA 扫描、审计一致性(06) |

| 文档 | 覆盖 | 主要方法 |
|---|---|---|
| [01-状态迁移表与状态机用例](01-状态迁移表与状态机用例.md) | 6 状态 × 6 目标 = 36 格,11 合法 25 非法;重开窗口 | 状态迁移法 + 边界值 |
| [02-SLA边界值分析](02-SLA边界值分析.md) | 截止时间计算、闭区间判定、跨天跨时区、防重复 | 边界值 + 场景法 |
| [03-LLM降级判定表](03-LLM降级判定表.md) | 熔断状态 × 客户端结果 × 契约校验 → 动作;两个调用点 | 判定表 + 等价类 |
| [04-权限判定表](04-权限判定表.md) | 角色 × 组 × 归属 → 放行 / 40301 / 40302;水平与垂直越权 | 判定表 |
| [05-附件等价类划分](05-附件等价类划分.md) | 扩展名 / 声明 MIME / 魔数三层各自的等价类;大小边界;路径穿越 | 等价类 + 边界值 |
| [06-MQ幂等与场景法](06-MQ幂等与场景法.md) | 投递、重投、不同消费者、业务失败;审计一致性 | 场景法 |
| [07-安全用例](07-安全用例.md) | 注入探测、鉴权头、敏感信息 | 等价类 |
| [08-并发与容量-竞态回归与故障注入](08-并发与容量-竞态回归与故障注入.md) | 抢单 / 指派 / 流转的并发结局判定表、审计序列反向断言、容量契约、草稿降级、故障注入、可观测性(修复阶段) | 场景法 + 判定表 + 反向断言 |

全部用例的清单(编号、层次、文件、方法)在 [../test-inventory.md](../test-inventory.md)。
